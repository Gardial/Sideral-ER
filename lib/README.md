# Local binary dependencies

This folder is intentionally not committed except for this note.

Place the local DLL dependencies required by `RandomMagicConversion.csproj` here before building from source.

See `BUILDING.md` for the expected file list.

